# 도메인 모델과 DDD

### 소프트웨어 개발에서 도메인이란?
- **사용자** 가 프로그램, 또는 소프트웨어 서비스를 적용하는 주제 영역을 도메인이라고 한다.
- 소프트웨어가 도메인을 반영하도록 만들어야 한다.
  - 개발자들은 개발하기 전 내가 만드는게 무엇인지. 어떠한 도메인을 가진 소프트웨어를 만드는 지 잘 이해하고 개발을 해야한다.

### 도메인 모델
- 소프트웨어는 도메인의 핵심 개념과 요소들을 통합하고, 그 관계를 정확하게 구현해야한다.
- 소프트웨어는 도메인을 모델링해야 한다.
- 도메인은 현실 세계의 일부임. 다 구현하지 못한다. 따라서, 도메인을 추상화하여 ***도메인 모델*** 을 만들어야한다.
- 도메인 모델은 소프트웨어가 해결하려는 특정 문제 영역(도메인)의 핵심 지도
- 도메인에 존재하는 중요한 개념과 이들 사이의 관계, 그리고 규칙을 표현한다.

### 도메인 주도 설게(DDD)
- **도메인 모델을 개발 과정의 중심**에 두는 방법이다.
- 개발자 뿐 아니라 도메인전문가들이 모두 참여해서 **함꼐 도메인 모델을 만들고 발전**시켜야 한다.
- **도메인 모델이 설계와 코드**까지 이어져야한다!!

해당 프로젝트는 DDD로 개발한만큼 복잡하지는 않다. 다만 DDD에 담긴 유용한 패턴과 실천법들을 적재적소에 사용할 예정이다. 

### 도메인 모델 만드는 절차
- 듣고 배우기
- **중요한 것**들 찾기 (개념 식별)
- **연결 고리** 찾기 (관계 정의)
- **것**들을 설명하기(핵심적인 속성 및 기본 행위 명시)
- 그려보기 (시각화 Ex- 클래스 다이어그램)
- 이야기하고 다듬기(반복)

### 도메인 전문가가 없다면
- 다양한 탐색과 집단적인 학습 - 이벤트 스토밍(함꼐 모여서 얘기하면서 포스트잇을 붙여 가면서 이야기를 나누는 것)
- 보편 언어의 의식적인 구축과 진화
- 테스터, 초기 사용자와 적극정인 소통
- 도메인 모델의 발전과 진화, 공유를 더욱 적극적으로 해야 한다