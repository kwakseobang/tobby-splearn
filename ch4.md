# CH 4

## Entity
- 도메인 모델을 만들 떄 사용하는 패턴
- 도메인 안에 있는 대상이나 개념
- ***고유한 식별자*** 를 가지고 이를 통해서 개별적으로 구분된다.
- ***생명 주기를 가진다***. 시간의 흐름에 따라 상태가 변경될 수 있다.

## 도메인 모델 패턴
- 도메인/비즈니스 로직을 구성하는 아키텍처 패턴의 한가지
- 도메인 모델의 속성과 행위를 모두 포함하는 도메인의 오브젝트 모델이다.
  - 속성과 행위가 한 곳에!! why? 도메인 모델이 가지고 있는 행위는 어떠한 속성을 사용해야함. 따라서 그 속성을 다루는 행위와 그 속성은 응집되어 있는 게 좋다.
- 오브젝트 모델이기 떄문에 복잡한 연관관계, Custom 속성, 상속 등을 사용할 수 있다.


## 파라미터 오브젝트 
-  여러 개의 매개변수를 하나의 객체로 묶어서 전달하는 리팩토링 기법
- 언제 사용하나?
  - 항상 같이 사용되는 매개변수가 있는 경우
  - 함수의 매개변수 개수가 너무 많아서 가독성이 떨어질 경우
- 장점
  - 코드가 줄어들어 가독성이 올라간다. 
  - 속성이 변경되어도 메소드 시그니처를 변경할 필요가 없다.


## test code tip!!
- 변수 앞 타입 선언을 하지 않고 var 를 사용함. 생성자로 타입이 명시되어 있는데 중복으로 표시할 필요가 없다고 생각하고. 더 중요한 것에 시선이 감.

## 자잘한 Tip
- 코드 작성 할떄 의미 있는 단위로 개행을 하자. 읽고 이해하기 편하다.
- 패키지 단위로 Default로 null을 허용하지 않게 하고 어노테이션을 이용해 부분 허용 하게 하기. 
  - package-info.java -> package 위에 @NonNullApi 
  - spotbug가 검증해줌 컴파일할때
- _Domain Service_
  - 일반적인 @Service가 붙은 서비스가 아니라 도메인 안에 소속되어 있는 건데 도메인 모델을 그대로 코드로 반영할 떄 만들어짐
보통 특정 엔티티 안에 집어넣기가 애매한 기능을 가지고 있는 것들이거나 뭔가 상세한 구현은 굳이 도메인 안에 담아둘 필요가 없는 것들을
그냥 인터페이스로 정의하여 사용함.
- 외부에서 도메인 서비스 같은걸 생성자에 주입할 떄는 정적 팩토리 메소드를 사용하는 편. 
  - 이점: 여러가지가 있지만 일단 이름을 부여할 수 있고 그 안에서 부가적인 작업이 가능함.
