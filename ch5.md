# 핵사고날 아키텍쳐

## 핵사고날 아키택쳐 사실
### 아키텍쳐

- 시스템의 기본적인 구조를 정의한다.
- 시스템의 중요한 품질 속성의 큰 영향을 미친다.
- 설계 결정에 기반이 되는 핵심적인 개념이다.
- 기본 구성 요소와 상호관계, 제약 조건, 원칙 등을 포함한다.

### 계층형 아키텍쳐(Layered Architecture)

- 서브 시스템을 **계층(layers)** 으로 구조화 하는 아키텍쳐 스타일이다.
- 계층은 **사용 관계**로 연결된다.(하나의 계층이 필요한 경우 다른 계층을 사용한다.)
- 사용 관계는 일반적으로 단방향이어야 한다는 핵심 제약이 있다.
    - 상위 계층이 하위 계층을 사용하는 하향식 흐름이다.
- 각 계층이 하위 계층의 내부 작동 원리를 알지 못하고 **공개된 인터페이스만을 사용한다**.(계층 격리)
    - 어떤 레이어의 변경이 다른 레이어의 컴포넌트에게 가능한 영향을 주지 않아야 한다.

### 핵사고날 아키텍쳐
- 대칭형 아키텍쳐
- 위 아래, 좌 우가 아닌 애플리케이션의 내부와 외부의 대칭 구조를 가진다.
- 헥사고날 아키텍쳐는 애플리케이션(내부)이 외부 기술에 의존하지 않는 애플리케이션 코드와 그 포트 인터페이스가 외부 기술에 의존하지 않아야함.

### 핵사곤의 내부
- 쉽게 변하지 않는, 중요한 도메인 로직을 담은 코어 어플리케이션
    - 도메인 로직을 가진 트랜잭션 스크립트
    - 애플리케이션 서비스와 도메인 패턴 모델을 따라 만든 도메인

### 핵사곤의 외부
- 핵사곤과 상호작용하는 모든 것 - Actor
- 사용자, 브라우저 등..
- 운영 환경, DB, 메세징 시스템, 메일 시스템 등..
- 테스트

### 핵사고날 아키텍처의 특징과 혜택

- 테스팅! 운영 시스템에 연결되지 않고 애플리케이션 테스트
- 애플리케이션과 상호작용하는 액터가 바뀌어도 재빌드 없이 테스트
- UI 디테일이나 기술 정보가 도메인 로직 안으로 노출되지 않도록 보호한다. 반대도 마찬가지.
- 컴포넌트를 각각 개발하고 연결하는 방식으로 큰 시스템을 분리할 수 있다.
- 시간이 지나면서 외부 연결을 다른 것으로 대체할 수 있다.
- 기술 요소를 제거했기에 도메인 설계에 집중할 수 있다.

### 애플리케이션은 어떻게 액터와 상호작용하는가??
- 포트를 이용해서 외부의 액터들과 소통을 한다.
- 단순히 데이터를 주고 받는 것이 아니라 명확한 목적과 방향을 가지고 외부와 연결된다.
- 애플리케이션이 정의한 **인터페이스** 로 만들어진다.

### Port(인터페이스)
- Lollipop: Provided interface
  - _기능 제공 인터페이스_
  - 애플리케이션이 제공하는 기능들을 사용하는 액터를 ***Primary Actor***라고 한다.
- Socket: Required interface
  - _기능 요구 인터페이스_
  - 애플리케이션이 동작을 하다가 어떤 기능이 필요한데 애플리케이션이 직접 가지고 있지 않을 때 그런 기능을 제공하는 외부 액터들과 소통하기 위해 쓰임 ***Secondary Actor***라고 한다.

### 어댑터(Adapter)
- 애플리케이션의 포트가 액터와 직접적으로 연결할 수 없다면 인터페이스 변환을 위한 어댑터를 도입한다.
- 브라우저를 통해서 애플리케이션의 회원 등록 포트의 기능 제공 인터페이스를 사용할려면?
  - 회원 가입 기능 제공 인터페이스를 사용하는 웹 컨트롤러 어댑터를 만든다.
- 애플리케이션의 회원 정보 저장 포트의 기능 요구 인터페이스로 DB와 직접 연결할수없다면?
  - 기능 요구 인터페이스를 구현한 리포지토리 어댑터를 만든다.

- Actor -> _Adapter_ -> Port -> Application(Hexagon)

### 헥사고날 아키택쳐의 비대칭성
- 어플리케이션이 제공하는 기능을 사용하는 액터와 이를 위한 어댑터
  - primary actor, primary adapter
- 어플리케이션이 동작하는데 필요한 기능을 제공하는 액터와 이를 위한 어댑터
  - secondary actor, secondary adapter

## 핵사고날 아키텍쳐가 요구하는 것
- 애플리케이션은 모든 외부와의 상호작용을 위해 `provided interface와 required interface`을 정의해서 가지고 있어야 함.
- 애플리케이션과 상호작용 하는 액터는 런타임에 구성되어야 한다.
- 애플리케이션은 액터에 대한 코드 의존성을 가지면 안된다.
- 액터는 정의된 포트를 통해서만 연결되어야 한다.
- 포트 인터페이스는 의도를 들어내는 코드만 있어야 하지 기술 의존성을 가지면 안된다.

## 핵사고날 아키택쳐 오해
### 애플리케이션 내부(육각형 내부)에 도메인 계층을 만들어야 한다.
- 헥사고날 아키택쳐는 **애플리케이션 내부 구현에 대한 원칙이나 요구 사항이** 없다.
  - 스파게티 코드로 만들어도 된다.
  - 트랜잭션 스크립트, 도메인 모델 패턴과 애플리케이션 서비스 등도 가능하다.
  - 도메인 계층을 포함한 아키텍처는 ***클린 아키텍처*** 이다.
- 헥사고날 아키택처는 클린 아키텍처가 아니다!

### 핵사고날 아키택쳐 패키지 구조를 따라야 한다.
- 헥사고날 아키택쳐가 **요구하는 패키지 구조**는 없다.
- **애플리케이션과 어댑터 패키지**를 분리하는 것은 바람직 하다.
- 애플리케이션에는 포트에 해당하는 인터페이스가 있음. 해당 포트를 구분된 패키지에 두는 걸 추천!

### 포트는 UseCase 라는 접미사를 사용한다.
- 포트의 의도를 담은 네이밍을 사용하면 된다.

### 애플리케이션에는 도메인 이름만 넣고 JPA 엔티티등은 어댑터에 따로 둬야한다.


## 스프런 개발에 적용할 아키텍처 패턴
- 헥사고날 아키텍처
- 도메인 모델 패턴

## 핵사고과 도메인 패턴을 적용한 대칭형 계층 구조
- 도메인 계층
- 애플리케이션 계층
- 어댑터 계층

- 외부에서 내부로 향하는 일종의 계층 구조
- 코드의 의존 방향은 내부로만 향한다. Adapter -> Application(Hexagon) -> Domain
- 이게 중요하다!!!!! 무조건 의존성은 외부에서 내부로 향한다.


## 엔티티 식별자
- 고유성
- 불변성